<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>AI家計簿＋傾斜割り勘</title>
</head>
<body>
  <h1>AI家計簿＋傾斜割り勘</h1>

  <h2>ユーザー作成</h2>
  <form action="/users" method="post">
    <input type="text" name="name" placeholder="ユーザー名" required>
    <button type="submit">追加</button>
  </form>

  <h2>ユーザー一覧</h2>
  <ul>
    <% users.forEach(user => { %>
      <li>
        <a href="/users/<%= user.id %>"><%= user.name %> の家計簿</a>
      </li>
    <% }) %>
  </ul>

  <hr>

  <h2>グループ作成</h2>
  <form action="/groups" method="post">
    <input type="text" name="name" placeholder="グループ名" required>
    <button type="submit">作成</button>
  </form>

  <h2>グループ一覧</h2>
  <ul>
    <% groups.forEach(group => { %>
      <li>
        <a href="/groups/<%= group.id %>"><%= group.name %> の精算</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>